/*

script.js

*/


document.addEventListener("DOMContentLoaded", function(event) {
    //do work

    // Get all cells with hashes
    $()

    // For each cell:

        // If cell before it is same:
            // Color both green
        // Else:
            //Color both red

});